"""Eval dashboard page: results vs. targets with sample sizes.

Skeleton only — the dashboard lands in W5.10.
"""

import streamlit as st

HONESTY_FOOTER = (
    "Every insight is traceable to examples; every recommendation is a "
    "hypothesis, not a performance claim."
)

st.title("Eval Dashboard")
st.caption("Measured results against targets, with sample sizes — directional, not definitive.")

st.info("Content built in later tasks.")

st.caption(HONESTY_FOOTER)

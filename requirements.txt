# Streamlit Community Cloud reads this file (it does not run `pip install -e .`).
# Keep in sync with pyproject.toml [project.dependencies].
#
# For a byte-identical environment, install requirements-lock.txt instead —
# a full `pip freeze` snapshot. Two shared dependencies have already broken
# subsystems when left to resolve freely (decision-log Entries #6 and #19).

pydantic==2.10.6
anthropic==0.49.0
chromadb==0.6.3
sentence-transformers==3.4.0
rank-bm25==0.2.2
scikit-learn==1.6.0
pandas==2.2.0
streamlit==1.41.0
arize-phoenix==8.0.0
arize-phoenix-evals==0.20.8
uvicorn==0.34.3
ragas==0.2.0
langchain-community==0.3.0
langchain-anthropic==0.3.0
nltk==3.9.1
datasets==3.2.0

# Local image generation (Entry #33). diffusers 0.40.0 requires
# huggingface-hub>=1.23, which breaks transformers/sentence-transformers
# (both need <1.0); 0.31.0 is the newest that coexists with the pinned
# embedding stack. ~4.8GB model weights download on first use.
diffusers==0.31.0
huggingface-hub==0.36.2
accelerate==1.14.0
matplotlib==3.10.0
mcp==1.6.0
openpyxl==3.1.5
python-dotenv==1.0.1

# F4: Streamlit Cloud's base image ships an sqlite too old for Chroma.
# The module-swap shim is at the top of app/shared.py.
pysqlite3-binary==0.5.4
